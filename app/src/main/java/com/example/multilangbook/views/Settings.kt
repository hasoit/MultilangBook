package com.example.multilangbook

/*
@Composable
fun settings() {
    menubox {
        Row {
            Text(
                "Comparison language: ",
                Modifier.padding(top = 12.dp, end = 8.dp),
                Color.Magenta,
                28.sp
            )

            dropdown(translationLanguages) {
                translator = Translator(TranslateLanguage.RUSSIAN, translationLanguages[it])
            }
        }

        intSetting(
            "Swipe treshold: $swipeTreshold",
            { swipeTreshold++ }, { swipeTreshold-- },
        )

        intSetting(
            "Amount: $amount",
            { increaseAmount() }, { decreaseAmount() }
        )

        switchButtons(
            SwitchButtonOptions("translation", showTranslations) {
                showTranslations = !showTranslations
            },
            SwitchButtonOptions("ruby", showRuby) {
                showRuby = !showRuby
            },
            SwitchButtonOptions("Transliterations", showTransliterations) {
                showTransliterations = !showTransliterations
            },
            SwitchButtonOptions("Compare", compare) {
                compare = !compare
            },
        )
    }
}
*/
